name: Gutenberg
requirements:
  running:
    unix:
      - imagemagick
      - graphicsmagick
framework:
  type: node
services:
  fs_gutenberg_photos: filesystem
ignores: ["node_modules", ".git", "uploads", ".tmp"]